import { ProfileCardFrame } from "@/components/ProfileCardFrame";

export default function CardTest() {
  return (
    <div style={{ display: "flex", justifyContent: "center", padding: 40 }}>
      <ProfileCardFrame userName="Sample Fan" />
    </div>
  );
}
